services:
  application:
    build: .
    depends_on:
      - redis
    volumes:
      - ./:/data
    restart: on-failure
    ports:
      - '8083:8083'
  redis:
    image: redis:latest
    restart: on-failure
    ports:
      - '6379:6379'