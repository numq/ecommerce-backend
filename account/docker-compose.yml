services:
  application:
    build: .
    depends_on:
      - mongo
    volumes:
      - ./:/data
    restart: on-failure
    ports:
      - '8082:8082'
  mongo:
    image: mongo:latest
    restart: on-failure
    ports:
      - '27017:27017'